/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const USER_STAKE_DISCRIMINATOR = new Uint8Array([
  102, 53, 163, 107, 9, 138, 87, 153,
]);

export function getUserStakeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(USER_STAKE_DISCRIMINATOR);
}

export type UserStake = {
  discriminator: ReadonlyUint8Array;
  /** PDA bump */
  bump: number;
  /** Reference to the stake config */
  stakeConfig: Address;
  /** User's public key */
  user: Address;
  /** Guardian pool this stake is delegated to */
  guardianPool: Address;
  /** Total shares owned by this user */
  shares: bigint;
  /**
   * Average cost basis (share price) at which user staked
   * This is a weighted average: cost_basis = (old_cost_basis * old_shares + new_share_price * new_shares) / total_shares
   */
  costBasis: bigint;
  /**
   * Amount of commission earned by the Guardian given the amount of shares staked.
   * Used as a reference point to calculate the amount of commission earned by the Guardian
   * while the user's stake is active.
   */
  cumulativeCommissionBeforeStaking: bigint;
  /** Token amount that is currently unstaking (calculated at unstake time using share price) */
  unstakingAmount: bigint;
  /** Unix timestamp when unstake was initiated (for cooldown calculation) */
  unstakeTimestamp: bigint;
};

export type UserStakeArgs = {
  /** PDA bump */
  bump: number;
  /** Reference to the stake config */
  stakeConfig: Address;
  /** User's public key */
  user: Address;
  /** Guardian pool this stake is delegated to */
  guardianPool: Address;
  /** Total shares owned by this user */
  shares: number | bigint;
  /**
   * Average cost basis (share price) at which user staked
   * This is a weighted average: cost_basis = (old_cost_basis * old_shares + new_share_price * new_shares) / total_shares
   */
  costBasis: number | bigint;
  /**
   * Amount of commission earned by the Guardian given the amount of shares staked.
   * Used as a reference point to calculate the amount of commission earned by the Guardian
   * while the user's stake is active.
   */
  cumulativeCommissionBeforeStaking: number | bigint;
  /** Token amount that is currently unstaking (calculated at unstake time using share price) */
  unstakingAmount: number | bigint;
  /** Unix timestamp when unstake was initiated (for cooldown calculation) */
  unstakeTimestamp: number | bigint;
};

/** Gets the encoder for {@link UserStakeArgs} account data. */
export function getUserStakeEncoder(): FixedSizeEncoder<UserStakeArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["bump", getU8Encoder()],
      ["stakeConfig", getAddressEncoder()],
      ["user", getAddressEncoder()],
      ["guardianPool", getAddressEncoder()],
      ["shares", getU128Encoder()],
      ["costBasis", getU128Encoder()],
      ["cumulativeCommissionBeforeStaking", getU128Encoder()],
      ["unstakingAmount", getU64Encoder()],
      ["unstakeTimestamp", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: USER_STAKE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link UserStake} account data. */
export function getUserStakeDecoder(): FixedSizeDecoder<UserStake> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["bump", getU8Decoder()],
    ["stakeConfig", getAddressDecoder()],
    ["user", getAddressDecoder()],
    ["guardianPool", getAddressDecoder()],
    ["shares", getU128Decoder()],
    ["costBasis", getU128Decoder()],
    ["cumulativeCommissionBeforeStaking", getU128Decoder()],
    ["unstakingAmount", getU64Decoder()],
    ["unstakeTimestamp", getI64Decoder()],
  ]);
}

/** Gets the codec for {@link UserStake} account data. */
export function getUserStakeCodec(): FixedSizeCodec<UserStakeArgs, UserStake> {
  return combineCodec(getUserStakeEncoder(), getUserStakeDecoder());
}

export function decodeUserStake<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<UserStake, TAddress>;
export function decodeUserStake<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<UserStake, TAddress>;
export function decodeUserStake<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<UserStake, TAddress> | MaybeAccount<UserStake, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getUserStakeDecoder(),
  );
}

export async function fetchUserStake<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<UserStake, TAddress>> {
  const maybeAccount = await fetchMaybeUserStake(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeUserStake<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<UserStake, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeUserStake(maybeAccount);
}

export async function fetchAllUserStake(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<UserStake>[]> {
  const maybeAccounts = await fetchAllMaybeUserStake(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeUserStake(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<UserStake>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeUserStake(maybeAccount));
}

export function getUserStakeSize(): number {
  return 169;
}
