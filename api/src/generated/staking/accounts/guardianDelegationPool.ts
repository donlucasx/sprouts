/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const GUARDIAN_DELEGATION_POOL_DISCRIMINATOR = new Uint8Array([
  133, 238, 255, 214, 215, 11, 189, 23,
]);

export function getGuardianDelegationPoolDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GUARDIAN_DELEGATION_POOL_DISCRIMINATOR,
  );
}

export type GuardianDelegationPool = {
  discriminator: ReadonlyUint8Array;
  /** Reference to the stake config */
  stakeConfig: Address;
  /** Guardian public key (used as PDA seed, immutable) */
  guardian: Address;
  /** Authority that can update guardian settings (can be changed) */
  authority: Address;
  /** Total shares delegated to this guardian */
  totalShares: bigint;
  /**
   * Per-guardian cumulative commission per share, tracked based on this guardian's commission rate.
   * This is updated when rewards arrive, ensuring commission is calculated based on rate at time rewards were earned.
   */
  cumulativeCommissionPerShare: bigint;
  /**
   * Last share price when this guardian's commission was last updated.
   * Used to calculate share price increases for commission tracking.
   */
  lastSharePrice: bigint;
  /** Commission tokens accrued by this guardian. */
  accruedCommission: bigint;
  /** Commission rate in basis points (0-10,000) */
  commissionBps: number;
  /** PDA bump */
  bump: number;
  /** Whether this guardian pool is active */
  active: boolean;
  /**
   * Share price at the time of deregistration (if inactive)
   * Used to calculate unstake amounts for inactive guardians
   * Set to 0 when active, and to the share price when inactive
   */
  deregisteredSharePrice: bigint;
};

export type GuardianDelegationPoolArgs = {
  /** Reference to the stake config */
  stakeConfig: Address;
  /** Guardian public key (used as PDA seed, immutable) */
  guardian: Address;
  /** Authority that can update guardian settings (can be changed) */
  authority: Address;
  /** Total shares delegated to this guardian */
  totalShares: number | bigint;
  /**
   * Per-guardian cumulative commission per share, tracked based on this guardian's commission rate.
   * This is updated when rewards arrive, ensuring commission is calculated based on rate at time rewards were earned.
   */
  cumulativeCommissionPerShare: number | bigint;
  /**
   * Last share price when this guardian's commission was last updated.
   * Used to calculate share price increases for commission tracking.
   */
  lastSharePrice: number | bigint;
  /** Commission tokens accrued by this guardian. */
  accruedCommission: number | bigint;
  /** Commission rate in basis points (0-10,000) */
  commissionBps: number;
  /** PDA bump */
  bump: number;
  /** Whether this guardian pool is active */
  active: boolean;
  /**
   * Share price at the time of deregistration (if inactive)
   * Used to calculate unstake amounts for inactive guardians
   * Set to 0 when active, and to the share price when inactive
   */
  deregisteredSharePrice: number | bigint;
};

/** Gets the encoder for {@link GuardianDelegationPoolArgs} account data. */
export function getGuardianDelegationPoolEncoder(): FixedSizeEncoder<GuardianDelegationPoolArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["stakeConfig", getAddressEncoder()],
      ["guardian", getAddressEncoder()],
      ["authority", getAddressEncoder()],
      ["totalShares", getU128Encoder()],
      ["cumulativeCommissionPerShare", getU128Encoder()],
      ["lastSharePrice", getU128Encoder()],
      ["accruedCommission", getU128Encoder()],
      ["commissionBps", getU16Encoder()],
      ["bump", getU8Encoder()],
      ["active", getBooleanEncoder()],
      ["deregisteredSharePrice", getU128Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: GUARDIAN_DELEGATION_POOL_DISCRIMINATOR,
    }),
  );
}

/** Gets the decoder for {@link GuardianDelegationPool} account data. */
export function getGuardianDelegationPoolDecoder(): FixedSizeDecoder<GuardianDelegationPool> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["stakeConfig", getAddressDecoder()],
    ["guardian", getAddressDecoder()],
    ["authority", getAddressDecoder()],
    ["totalShares", getU128Decoder()],
    ["cumulativeCommissionPerShare", getU128Decoder()],
    ["lastSharePrice", getU128Decoder()],
    ["accruedCommission", getU128Decoder()],
    ["commissionBps", getU16Decoder()],
    ["bump", getU8Decoder()],
    ["active", getBooleanDecoder()],
    ["deregisteredSharePrice", getU128Decoder()],
  ]);
}

/** Gets the codec for {@link GuardianDelegationPool} account data. */
export function getGuardianDelegationPoolCodec(): FixedSizeCodec<
  GuardianDelegationPoolArgs,
  GuardianDelegationPool
> {
  return combineCodec(
    getGuardianDelegationPoolEncoder(),
    getGuardianDelegationPoolDecoder(),
  );
}

export function decodeGuardianDelegationPool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<GuardianDelegationPool, TAddress>;
export function decodeGuardianDelegationPool<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<GuardianDelegationPool, TAddress>;
export function decodeGuardianDelegationPool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<GuardianDelegationPool, TAddress>
  | MaybeAccount<GuardianDelegationPool, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getGuardianDelegationPoolDecoder(),
  );
}

export async function fetchGuardianDelegationPool<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<GuardianDelegationPool, TAddress>> {
  const maybeAccount = await fetchMaybeGuardianDelegationPool(
    rpc,
    address,
    config,
  );
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeGuardianDelegationPool<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<GuardianDelegationPool, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeGuardianDelegationPool(maybeAccount);
}

export async function fetchAllGuardianDelegationPool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<GuardianDelegationPool>[]> {
  const maybeAccounts = await fetchAllMaybeGuardianDelegationPool(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeGuardianDelegationPool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<GuardianDelegationPool>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeGuardianDelegationPool(maybeAccount),
  );
}

export function getGuardianDelegationPoolSize(): number {
  return 188;
}
